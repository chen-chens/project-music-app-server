/**
 * 自定義錯誤類型：
 * (1) Express 啟用錯誤
 * (2) User Input 錯誤：泛指各種前端傳來的個各種錯誤
 * (3) 資料庫連結 或 資料庫處理錯誤
 * (4) 其他客製錯誤 >> 直接在對應的 controller 內 進行個別處理。
 */

// export interface ApplicationError extends Error {}
// export interface ClientSideError extends Error {}
// export interface DatabaseError extends Error {}
// export interface CustomedByCaseError extends Error {}

// class ApplicationError extends Error {}
